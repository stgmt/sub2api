---
name: sub2api-claude-code-codex
description: >-
  Operate Claude Code through local Headroom + sub2api with OpenAI/Codex subscription and optional Alibaba Token Plan routing. Use for Docker/WSL or Ubuntu/Hyper-V setup, host RTK, CUDA Kompress, embedding-server/watchdog, Codex OAuth self-heal, GPT-5.6, Qwen/GLM/DeepSeek, compact and subagent routing, picker aliases, effort/context mapping, persistence/autostart, and request tracing. Also use for empty or broken SSE streams, mixed-memory tool-call errors, stale fake 429/503 cooldowns, no available accounts, refresh_token_reused, local Headroom rate limits, context-mode hangs, and runtime/source mismatches. Triggers include "Claude Code через Codex подписку", "Headroom sub2api", "qwen3.8-max-preview", "usage 0/0", "503 Service temporarily unavailable", and "No tool output found".
---

# sub2api Claude Code Codex

This skill is the short entrypoint for running Claude Code through a local Headroom + `sub2api` Anthropic-compatible proxy chain backed by the user's OpenAI/Codex/ChatGPT subscription. Keep this file lean; load the reference files only when the task needs those details.

## Current Profile

Use this profile unless the user explicitly asks for a different model, port, or risk tradeoff:

```text
Fork: https://github.com/stgmt/sub2api
Headroom fork: https://github.com/stgmt/headroom
Branch: main
Verified main commit: use `git log -1 --oneline` on https://github.com/stgmt/sub2api main after each stack update
Fixed issue: https://github.com/stgmt/sub2api/issues/1
Image: sub2api-codex:local-token-usage
Headroom image: headroom-sub2api:0.31.0 built from `HEADROOM_GIT_REPO=https://github.com/stgmt/headroom.git` at pinned `HEADROOM_GIT_REF` using `HEADROOM_RUST_TOOLCHAIN=1.88.0`, plus RTK, lean-ctx, TokenSave, ast-grep, difft, scc, and idempotent downstream embedding-server plus Claude Code streaming-overlap patch guardrails. NVIDIA installs use `docker-compose.gpu.yml`, the `gpu` image stage with pinned CUDA PyTorch, and Headroom's native batched `pytorch` Kompress backend; CPU remains the fallback.
Docker compose project: sub2api-codex
Deploy profile: deploy/claude-code-codex-headroom
Claude base URL: http://127.0.0.1:8787
Claude chain: Claude Code -> Headroom 127.0.0.1:8787 -> Docker DNS http://sub2api:8080
Direct sub2api URL: http://127.0.0.1:18081 for admin UI, diagnostics, and non-Claude clients only
Headroom MCP: user-level Claude MCP named `headroom`, launched through `wsl.exe -e docker exec -i headroom-sub2api headroom mcp serve --proxy-url http://127.0.0.1:8787`
Headroom savings profile: agent-90, target ratio 0.10, context tool RTK, code-aware compression on, memory on, embedding server on, output shaper on, effort router off so client-selected effort is preserved
RTK execution: pinned RTK 0.42.4 on Windows plus WSL or on the native Linux host that runs Claude Code; use one global Claude Code `PreToolUse(Bash)` hook and share host history with Headroom at `/root/.local/share/rtk` when both run on the same Docker host
Host state root: `${SUB2API_STATE_ROOT:-./data}` in the deploy profile. All state must be host bind-mounted there, not stored in Docker named volumes.
Codex OAuth self-heal: sub2api reads `SUB2API_OPENAI_CODEX_AUTH_FILE=/app/data/codex-auth.json`; setup and the repeating self-heal task copy a validated host `%USERPROFILE%\.codex\auth.json` into `${SUB2API_STATE_ROOT}/sub2api/codex-auth.json` without printing secrets.
Headroom persistence: `/root/.headroom` stores `ccr_store.db`, savings, and logs; `/root/.cache/headroom` and `/root/.cache/huggingface` store warmed local tool/model/embedding caches; `/root/.local/share/rtk` exposes the host RTK history to Headroom. These paths must be host bind mounts.
Windows autostart/self-heal: use one scheduled task named `Sub2API Codex Proxy Stack Autostart`, not a Startup-folder `.cmd` and not a separate `headroom-proxy` task. The task must launch `ensure-sub2api-proxy-stack.ps1` through the zero-window `run-hidden.vbs`/`wscript.exe` wrapper with `RunLevel=Highest` at logon and every minute; direct interactive `powershell.exe` actions can flash a console and steal focus. Its healthy path probes same-host plus diagnostic Hyper-V bridge `/health`; the bridge is fail-closed only with `HEADROOM_HYPERV_REQUIRE_BRIDGE=1` or `-RequireHyperVBridge`. Its failure path calls the canonical stack start script to wake WSL, restore Docker compose, refresh dynamic bridge addresses, and self-heal stale WSL `ext4.vhdx` attach locks.
Main model: gpt-5.6-sol
Small-fast/compact/delegated SDK model: qwen3.8-max-preview with effort high
Published model families: GPT/Codex plus Alibaba Token Plan only; do not expose real Claude/Fable passthrough aliases in `/v1/models` for this profile
Picker category defaults: Opus/Fable/Sonnet/Haiku are pinned to Qwen high (`qwen3.8-max-preview`)
Provider families: GPT/Codex models route to the OpenAI/Codex OAuth account; qwen*/glm*/deepseek-v4-pro route to the Alibaba Token Plan Anthropic-compatible account.
Official model windows: GPT-5.6 Sol/Terra/Luna = 1.05M; GPT-5.3-Codex-Spark = 128k and text-only; Claude Fable 5/Opus 4.8/Sonnet 5 = 1M; Claude Haiku 4.5 = 200k
Client context target: CLAUDE_CODE_MAX_CONTEXT_TOKENS=370000, CLAUDE_CODE_AUTO_COMPACT_WINDOW=340000 (Claude Code client compact/display target; lower than the official 1.05M GPT-5.6 window to avoid late upstream overflow and max-output failures)
Compact model: qwen3.8-max-preview through group messages_dispatch_model_config.compact_mapped_model; compact requests must cross-route from GPT/Codex to the Alibaba Anthropic-compatible account before provider classification
Reasoning: main interactive GPT-5.6 uses the effort selected by Claude Code; all user-level subagent overrides and every `claude -p` / `--print` SDK request use qwen3.8-max-preview with effort high
```

Claude Code model routing:

```text
GPT/Codex: gpt-5.6-sol, gpt-5.6-terra, gpt-5.6-luna, gpt-5.6-terra-medium, gpt-5.3-codex-spark, and legacy gpt-* route to OpenAI/Codex.
Alibaba Token Plan: qwen3.8-max-preview, qwen3.7-max, qwen3.7-plus, qwen3.6-flash, glm-5.2, deepseek-v4-pro route through the Anthropic-compatible Token Plan account.
Claude Code picker aliases: Opus/Fable/Sonnet/Haiku are UI slots and should resolve to `qwen3.8-max-preview`; raw `opus`, `fable`, `sonnet`, `haiku`, and `claude-*` are not published as provider models in this local profile.
Subagent, standalone `claude -p`, and compact override: qwen3.8-max-preview with effort high. The group-level `sdk_cli_mapped_model` rule covers both `Agent(...)` and print-mode processes identified by `claude-cli/... (external, sdk-cli...)`; interactive `(external, cli)` requests keep their selected model. The legacy `gpt-5.6-terra-medium` alias remains supported but is no longer the default delegated-agent profile.
```

## Reference Map

Read only the file needed for the current task:

- `references/profile-and-context.md`: source-of-truth profile, official GPT-5.6 context facts, compact profile, and context-window caveats.
- `references/install-and-claude-config.md`: Docker/WSL install, OAuth import, Claude Code env/settings, dynamic MCP, and memory/rules configuration.
- `references/group-and-compact-routing.md`: sub2api group JSON, Claude model mapping, compact mapping, fallback SQL, and account credential patches.
- `references/subagent-terra-medium-profile.md`: historical Terra-medium delegated-agent profile plus the current Qwen-high override, exact files/env knobs to set, missing-override behavior, and future GPT-5.7 migration checklist.
- `references/workflow-qwen-high-rationale.md`: why workflow agents, subagents, compact, small-fast, and picker aliases are pinned to Qwen high; read before changing those defaults or reverting Spark/Terra/Haiku behavior.
- `references/verification.md`: health probes, Claude Code probes, `/v1/messages` checks, usage_logs queries, compact verification, and expected evidence.
- `references/troubleshooting.md`: 429/503/no-available-accounts, stale cooldowns, empty streams, context overflow, Luna availability, localhost relay, and usage-display bugs.
- `references/headroom-gpu-kompress.md`: GPU backend research, fork ownership, implementation, reproducible CPU/CUDA benchmark, autostart failure, live proof, and remaining architecture work.
- `references/session-failure-registry.md`: cross-session failure IDs, layer-specific root causes, forbidden shortcuts, and the proof required before declaring each class fixed.
- `references/fullpower-profile.json`: machine-readable profile snapshot when scripts or exact config values are useful.

## Operating Rules

- Do not print OAuth tokens, API keys, refresh tokens, passwords, or copied auth files in chat.
- Before changing a failing stack, match the symptom against `references/session-failure-registry.md` and build one correlated trace from Claude Code through Headroom and sub2api to the selected provider. Do not diagnose from a terminal label, health endpoint, model catalog, source diff, or UI effort/context display alone. If the user asked for analysis/report only, stop after evidence and recommendations; do not run broad suites or mutate runtime state.
- Do not wait for the user to catch persistence regressions. For every install, repair, Docker compose, Headroom, embedding-server, cache, Postgres, Redis, or "make reusable" task, proactively audit persistence before claiming done. The required proof is `docker inspect` showing `Type=bind` for Headroom `/root/.headroom`, `/root/.cache/headroom`, `/root/.cache/huggingface`, sub2api `/app/data`, Postgres parent `/var/lib/postgresql`, and Redis `/data`, plus `CCR_STORE True <nonzero>` after memory traffic. If any state path is a Docker named volume, fix the compose/profile/scripts first, recreate only the affected services, rerun `scripts/verify-claude-code-sub2api.ps1`, sync the local skill, commit, and push when repo changes were made.
- Prefer request-time `/v1/messages` probes and `usage_logs.upstream_model` over `/v1/models` catalog output.
- Current user-requested delegated profile is Qwen high everywhere: `ANTHROPIC_SMALL_FAST_MODEL=qwen3.8-max-preview`, `CLAUDE_CODE_SUBAGENT_MODEL=qwen3.8-max-preview`, and user-level `general-purpose`, `Explore`, `workflow-subagent`, `bench-reviewer`, and `bench-triage` frontmatter use `model: qwen3.8-max-preview` plus `effort: high`.
- Treat `claude -p` / `claude --print` as delegated SDK traffic too. The OpenAI group must set `messages_dispatch_model_config.sdk_cli_mapped_model=qwen3.8-max-preview` and `sdk_cli_reasoning_effort=high`. sub2api applies this before provider classification for `claude-cli/... (external, sdk-cli...)`, including Agent SDK children and explicit `--model` print calls. Do not match `(external, cli)`: interactive model selection must remain usable.
- Apply that profile on every OS that can launch Claude Code, not only on the machine that runs Docker. Use `scripts/sync-claude-subagent-profile.ps1` for each Windows user and `scripts/sync-claude-subagent-profile.sh` for native Linux/WSL users, then run the same script in check-only mode. The sync preserves existing settings hooks and agent bodies. For firewalled Windows Hyper-V guests, the elevated watchdog can stage the portable PowerShell sync through Guest Service Interface when `HEADROOM_HYPERV_STAGE_QWEN_PROFILE=1` and `HEADROOM_HYPERV_QWEN_VM_NAMES` lists the target VMs. A successful `Copy-VMFile` marker proves staging only; the guest launcher applies on its next interactive logon, and already-open Claude processes still require restart.
- Treat `%USERPROFILE%\.local\bin\claude.cmd` as a higher-precedence runtime config when it contains `set "..."` model lines. `settings.json` and User env can both be correct while this wrapper silently restores Spark/Terra or another stale delegated model on every launch. Setup must run `scripts/sync-claude-wrapper-models.ps1`; verification must run it with `-CheckOnly`; final proof must be a real parent `Agent` invocation or Claude JSON `modelUsage`, not `claude --agent`, because `--agent` turns that definition into the main session and does not exercise `CLAUDE_CODE_SUBAGENT_MODEL` semantics.
- Set picker Opus/Fable/Sonnet/Haiku defaults to Qwen high: `ANTHROPIC_DEFAULT_OPUS_MODEL=qwen3.8-max-preview`, `ANTHROPIC_DEFAULT_FABLE_MODEL=qwen3.8-max-preview`, `ANTHROPIC_DEFAULT_SONNET_MODEL=qwen3.8-max-preview`, and `ANTHROPIC_DEFAULT_HAIKU_MODEL=qwen3.8-max-preview`, with display name `Qwen 3.8 Max`.
- Keep hidden group compatibility mappings from bare and dated Claude aliases (`opus`, `fable`, `sonnet`, `haiku`, `claude-opus-4-8`, `claude-sonnet-4-6`, `claude-haiku-4-5`, `claude-haiku-4-5-20251001`) to `qwen3.8-max-preview`. They must be applied before mixed-provider classification but omitted from `/v1/models`; this protects stale Hyper-V/Windows clients without reintroducing Claude models into the picker.
- Keep normal message `model_fallbacks` empty unless the user explicitly asks for a fallback. Token Plan aliases must stay on the Alibaba account. For the current Qwen-high profile, compact uses group `compact_mapped_model`; account-level `compact_model_mapping` and `compact_model_fallbacks` should be empty to avoid silently reviving legacy Spark/Luna compact routing.
- Treat Claude Code `count_tokens` as part of the mixed-provider contract. Qwen/GLM/DeepSeek Token Plan preflight must not hit the OpenAI/Codex account; patched sub2api routes `/v1/messages/count_tokens` through the same model-family classifier and returns a local estimate for Token Plan models. A 400 like `The 'qwen3.8-max-preview' model is not supported when using Codex with a ChatGPT account` means the running image or route is stale.
- Native `/compact` recognition is a two-proxy contract. Headroom must preserve the exact compact message, add `x-sub2api-claude-compact: 1`, and skip output shaping; sub2api treats that header as authoritative. For the current mixed-provider profile, group `messages_dispatch_model_config.compact_mapped_model=qwen3.8-max-preview` rewrites GPT/Codex compact requests before provider classification so the compact uses the Alibaba Token Plan account with `reasoning_effort=high`.
- For Claude Code `general-purpose`, `Explore`, `workflow-subagent`, `bench-reviewer`, and `bench-triage`, use user-level overrides at `%USERPROFILE%\.claude\agents\*.md` with `model: qwen3.8-max-preview` and `effort: high`. Normal message fallbacks stay empty unless the user explicitly enables them. Verify with agent JSONL and `usage_logs.reasoning_effort`, not with the Claude UI label alone.
- Do not persist `CLAUDE_CODE_EFFORT_LEVEL` in Windows User/System env or `~/.claude/settings.json env`. It overrides Claude Code's interactive `/effort` command for every session. Use `effortLevel` only as a soft startup default, and prefer `xhigh` for the profile default so users can switch to `max`, `high`, or lower efforts in-session.
- Keep `HEADROOM_OUTPUT_SHAPER=1` but force `HEADROOM_EFFORT_ROUTER=0`. Headroom's router defaults on when omitted and classifies clean `tool_result` turns as mechanical, rewriting an explicit Claude Code `output_config.effort=max` to `low`. Claude's JSONL still records the session effort as `max`, so UI/transcript proof alone is false-positive evidence. Verify a multi-turn tool loop with a pre-Headroom wire tap or equivalent request-body capture and match each turn to `usage_logs.reasoning_effort`.
- Do not install a global `PreToolUse` / `SubagentStart` / `SubagentStop` hook that blocks Agent calls unless the user explicitly asks for it. Current policy is advisory only: `workflowSizeGuideline=small` plus the `general-purpose` prompt asks for no more than 10 sibling agents and no deep chains. Do not claim Claude Code has a reliable built-in depth cap that prevents hundreds of descendants; local evidence has shown a single parent line can grow into hundreds of spawned agents.
- Never describe `400000`, `370000`, or `340000` as the GPT-5.6 upstream/model context limit. These are Claude Code client compact/display targets. Current safe client profile is `CLAUDE_CODE_MAX_CONTEXT_TOKENS=370000` and `CLAUDE_CODE_AUTO_COMPACT_WINDOW=340000`; still verify real upstream failures from proxy logs before blaming context.
- Treat `stale fake 429/503` as a proxy state bug first. For OpenAI OAuth 403, require adaptive `2s -> 15s -> 1m -> 5m` cooldowns, one bounded same-request retry after the first 403, and success cleanup that removes only the OAuth 403 temp state. For quota-origin `model_rate_limits`, a successful reset-credit call must clear the persisted limit and scheduler snapshot immediately; an external ChatGPT reset must be discovered by a same-model bounded reprobe after one minute, with a fresh upstream 429 closing that reprobe window again. Do not introduce a model fallback unless the user asks for one. A ten-minute first-hit cooldown, permanent OAuth disable, or manual DB reset as routine operation means the image is stale or regressed. Issue #1 separately covers quota-origin model cooldown recovery.
- Treat `refresh_token_reused`, `invalid_refresh_token`, and similar `OPENAI_OAUTH_TOKEN_REFRESH_FAILED` errors as a Codex auth-file recovery path before surfacing a 503. Current fork behavior imports a fresh non-expired host Codex auth file, clears `status=error`, clears temp-unschedulable state, sets `schedulable=true`, invalidates stale token cache, and retries OpenAI selection once inside the original request. If the host auth file is missing, unchanged, or expired, return `401 authentication_error` telling the user to run `codex login`; do not hide it as `503 Service temporarily unavailable` or fix it with routine manual SQL.
- Treat `429 {"detail":"Rate limited. Retry after ..."}` as Headroom's local token bucket, not an upstream subscription cooldown. Claude Code can expose it inside a tool result without replaying the lost call. The loopback profile must write `HEADROOM_RPM=6000` and `HEADROOM_TPM=100000000`; prove the effective `/stats.rate_limiter` values and run `scripts/test-headroom-rate-limit-burst.mjs` after every recreate. Do not accept source/env inspection alone.
- For Docker-in-WSL, verify both Headroom and sub2api health plus the Windows route. If Windows cannot reach `127.0.0.1:8787` but WSL/Docker can, publish Headroom on `0.0.0.0` and use the WSL eth0 IP on port `8787`; do not point Claude Code directly at sub2api `18081` except as a temporary diagnostic bypass. For a Hyper-V Claude host, configure `hyperv-bridge.env`; the single elevated autostart task must refresh the Default Switch -> current WSL `v4tov4` bridge, restrict its firewall rule to the current VM IP, update the VM settings over SSH, and prove health from that VM. A Docker health check from Windows is not sufficient.
- For a Windows Hyper-V Claude host without SSH, set `HEADROOM_HYPERV_REMOTE_CONFIG_MODE=none` and `HEADROOM_HYPERV_REQUIRE_BRIDGE=1`. The sidecar env overrides stale VM arguments embedded in the scheduled task, and the task proves the stable Default Switch bridge while deliberately skipping guest mutation. The guest must use that stable switch URL, not the ephemeral WSL address. A WindowsApps `python` permission error is a separate guest-local hook-runtime failure.
- Keep `CLAUDE_CODE_DISABLE_NONSTREAMING_FALLBACK=1` in every Claude host, including native Linux/Hyper-V. Native compact remains intentionally non-streaming and is distinguished by the compact marker and Qwen compact route. A normal Sol request with `stream=false`, no compact marker, multi-megabyte body growth, and repeated empty terminal responses is an emergency-fallback regression; trace it through `ops_error_logs` and the Claude transcript instead of mislabeling it as compact or adding an unsolicited model fallback.
- Treat `/bin/sh: node: not found` or `/bin/sh: python: not found` from `UserPromptSubmit` as a Claude-host hook runtime failure, separate from the inference gateway. On native Linux/Hyper-V, verify `node`, `python`/`python3`, the generated `~/.dev-pomogator/scripts/tsx-runner.js`, and its local `tsx` dependency from the same user and project before debugging Headroom. Prove the repair with a fresh `claude --print` process; an already-open process can retain the old hook registry and environment.
- Keep Headroom, lean-ctx, and TokenSave service/MCP binaries inside Docker for this profile. RTK is the deliberate exception: it must be installed in the OS where Claude Code executes Bash. Use `scripts/install-claude-rtk.ps1` for Windows plus WSL, or `scripts/install-claude-rtk.sh` for a native Ubuntu/Linux Claude host such as a Hyper-V VM. Keep exactly one global `PreToolUse(Bash)` hook, use `MSYS2_ARG_CONV_EXCL='*'` only on the Windows-to-WSL bridge, preserve the `cat`, `git diff`, `git show`, and `curl` accuracy exclusions, and bind the host RTK state to Headroom `/root/.local/share/rtk` when applicable. If `claude mcp list` shows stale host `headroom` or `tokensave` entries, remove them and re-add only Docker-backed Headroom.
- Windows PowerShell 5.1 must not pipe the synthetic RTK JSON payload directly into Git Bash. Its native stdin encoding can make `rtk hook claude` see empty or invalid JSON. Installer and verifier probes must use `Invoke-GitBashUtf8Stdin`, which base64-encodes UTF-8 in PowerShell and decodes it inside Git Bash before invoking the exact installed hook command.
- Keep `--embedding-server` enabled. The Headroom image must build from `stgmt/headroom` by `HEADROOM_GIT_REPO/HEADROOM_GIT_REF`; the local patch scripts remain as idempotent guardrails for overridden refs. If startup logs show `Falling back to per-worker embedder`, rebuild from the fork-aware Dockerfile and verify the ref labels.
- Treat `400 No tool output found for function call call_*` on a fresh Claude Code prompt as a Headroom mixed-memory continuation bug before blaming the VM or sub2api. The fork must defer client-owned calls when `memory_search` is emitted beside `Bash`/`Read`, replay only result-backed memory calls, and remove private memory definitions from the continuation. Prove it from the Claude JSONL, Headroom `Memory: Deferred ... client-owned tool call(s)` marker, and sub2api HTTP 200 continuation rather than by retrying the session.
- On NVIDIA hosts, set `HEADROOM_ACCELERATOR=cuda`, `HEADROOM_DOCKER_TARGET=gpu`, and `HEADROOM_KOMPRESS_BACKEND=pytorch`, and include `docker-compose.gpu.yml` for every build, start, and autostart. Do not claim GPU from host `nvidia-smi` alone: require non-empty Docker `DeviceRequests`, `torch.cuda.is_available()`, the expected device name, and `KompressCompressor().preload(...)=pytorch`. Use `benchmark-headroom-kompress` for CPU/GPU A/B; compare identical payloads, compression ratio, and sentinel retention.
- Treat a confirmed CUDA profile as sticky. `auto` may upgrade a portable install to CUDA, but must not silently rewrite persisted `cuda` to CPU after a transient WSL/NVIDIA probe failure. The GPU compose overlay owns `target: gpu`, `gpus: all`, `HEADROOM_KOMPRESS_BACKEND=pytorch`, `HEADROOM_FORCE_KOMPRESS=1`, and `HEADROOM_DISABLE_KOMPRESS=0`; never use a bare base-compose launcher on that host.
- The verifier must not classify a failed `docker inspect` as a CPU profile. Run WSL Docker probes through the selected distro with bounded retry for `Wsl/Service/0x8007274c`, then require a successful env inspection before deciding whether CUDA verification applies.
- If WSL/Docker GPU access is blocked or Docker reports no CUDA `DeviceRequests`, do not leave CPU ONNX Kompress on the Claude Code hot path. Set `HEADROOM_FORCE_KOMPRESS=0`, `HEADROOM_DISABLE_KOMPRESS=1`, pass `HEADROOM_DISABLE_KOMPRESS` through compose, recreate Headroom, and verify `/health` reports `kompress.status=disabled` with `compression_executor.queued=0` and no leaked threads. Re-enable Kompress only after the GPU proof passes.
- Keep all service state on host bind mounts under `${SUB2API_STATE_ROOT:-./data}`. Headroom uses `headroom`, `headroom-cache`, and `headroom-huggingface`; sub2api uses `sub2api`; Postgres uses `postgres`; Redis uses `redis`. Do not replace these with Docker named volumes. Do not delete the state root unless the user explicitly wants to wipe memory, embeddings, accounts, database, and cache.
- When refreshing Codex/OpenAI OAuth after `Token revoked` / `account_disabled_auth_error`, verify both `status='active'` and `schedulable=true`. A recovered account with `schedulable=false` is still invisible to the scheduler and will surface as `no available accounts`, `503`, or Claude Code `Request timed out`. Current fork behavior restores `schedulable=true` from `ClearAccountError`, including the `apply-oauth-credentials` path.
- Keep Windows autostart single-owner. Remove or disable stale `headroom-proxy` scheduled tasks and Startup-folder `sub2api/headroom` `.cmd` launchers. A logon-only task is insufficient: the surviving task must have a one-minute repeating trigger, `RestartCount >= 3`, `MultipleInstances=IgnoreNew`, and call the health-first ensure script. The ensure script must not restart a healthy same-host stack merely because an optional Hyper-V bridge is down; require bridge fail-closed explicitly with `HEADROOM_HYPERV_REQUIRE_BRIDGE=1`. After `ConnectionRefused`, WSL poweroff, Docker loss, or required stale Hyper-V `portproxy`, it calls the full compose/bridge recovery and verifies required routes before succeeding. Normalize UTF-16/NUL WSL errors and retry transient `Wsl/Service/0x8007274c` failures on every WSL command, not only the initial Docker readiness probe.
- Keep the Headroom image bootstrap wrapper. `/usr/local/bin/start-headroom-proxy` seeds fresh persistent mounts from `/opt/headroom-seed` before starting `headroom proxy`, so a clean volume preserves memory without hiding bundled RTK/lean-ctx/difft/scc assets.
- Verify Headroom optimization with `docker exec headroom-sub2api headroom tools doctor`, `headroom savings --json`, and `headroom perf --format json`. A healthy stack should show bundled tools on PATH and nonzero proxy savings once traffic has passed through Headroom.
- Treat `/stats.requests`, `/stats.tokens`, and `/stats.latency` as current-runtime counters. Cross-restart analytics must come from `/stats.request_history` or the nested `lifetime` blocks restored from bind-mounted `logs/proxy-requests.jsonl`. Verify stable totals across restart and exact +1 after one fresh request; `/stats/reset` must remain runtime-only.
- After code changes, rebuild `sub2api-codex:local-token-usage` or the compose profile, recreate affected services under project `sub2api-codex`, then re-run live probes through Headroom.

## Workflow

1. Locate the exact Claude session/request, read `references/session-failure-registry.md`, classify the failing layer and incident ID, then inspect current state: `~/.claude/settings.json`, User env, Docker container health, group mapping, account `credentials` compact mappings, and account `extra.model_rate_limits`.
2. If installing or repairing Docker/Claude config, read `references/install-and-claude-config.md` and use the bundled setup script.
3. If the task touches persistence, embeddings, caches, Docker volumes, compose, setup, RTK, or "reusable" docs, run the host-bind audit before and after changes. Do not accept Docker named volumes as equivalent to host persistence. For RTK, require a real Claude Code Bash tool call that increments host `rtk gain` plus matching container totals; a manual container probe is not proof of automatic participation.
4. If changing models or compact routing, read `references/group-and-compact-routing.md`, patch `groups.messages_dispatch_model_config.compact_mapped_model`, and clear any stale OpenAI account `compact_model_mapping` / `compact_model_fallbacks` unless the user explicitly requests a legacy OpenAI-only compact fallback.
5. If debugging limits or errors, read `references/troubleshooting.md` before changing DB state. Preserve non-quota reasons such as `upstream_404_model_not_found` unless deliberately re-probing that model.
6. Verify with `references/verification.md`: Headroom health/upstream, host bind mounts, sub2api health, GPT-5.6 probes through Headroom, Claude alias probes, and `usage_logs` mapping evidence.
7. Update this skill only after live verification when a model lineup, context window, persistence layout, or proxy behavior changes.

## Bundled Scripts

- `scripts/setup-sub2api-claude-code.ps1`: create/update `deploy/claude-code-codex-headroom/.env`, auto-detect CUDA unless explicitly set to CPU, select the matching compose overlay/image target/backend, start the Headroom + sub2api compose project, install/update the single Windows scheduled-task autostart unless `-SkipAutostart` is passed, register the Docker-backed Headroom MCP, remove stale host `tokensave` MCP, sync a validated host Codex auth file to `${SUB2API_STATE_ROOT}/sub2api/codex-auth.json`, and configure Claude Code settings. Defaults to main `gpt-5.6-sol`, small-fast/subagent/compact `qwen3.8-max-preview` high, `SUB2API_OPENAI_CODEX_AUTH_FILE=/app/data/codex-auth.json`, `HEADROOM_SAVINGS_PROFILE=agent-90`, `HEADROOM_TARGET_RATIO=0.10`, `HEADROOM_RPM=6000`, `HEADROOM_TPM=100000000`, `CLAUDE_CODE_MAX_CONTEXT_TOKENS=370000`, and `CLAUDE_CODE_AUTO_COMPACT_WINDOW=340000`.
- `scripts/sync-claude-wrapper-models.ps1`: synchronize or audit higher-precedence model assignments in an existing Windows `claude.cmd` wrapper so stale launcher values cannot override Qwen small-fast, picker, and subagent settings.
- `scripts/sync-claude-subagent-profile.ps1`: portable Windows host/guest repair for Qwen small-fast, picker aliases, global delegated-agent files, User env, settings, and an existing wrapper; supports `-CheckOnly`.
- `scripts/sync-claude-subagent-profile.sh`: portable native Linux/WSL equivalent; preserves hooks and agent bodies, writes `environment.d`, and supports `--check`.
- `scripts/sync-sub2api-sdk-cli-routing.ps1`: idempotently set or audit the group-level `sdk_cli_mapped_model` and `sdk_cli_reasoning_effort` fields without replacing compact, exact mappings, or fallback JSON.
- `scripts/test-claude-subagent-profile-contract.ps1` and `.sh`: regression coverage for stale-profile repair, hook/body preservation, main-model preservation, and idempotent check-only verification.
- `scripts/test-claude-wrapper-model-contract.ps1`: regression-test stale-value detection, repair, insertion of missing picker aliases, and the final check-only contract on a temporary wrapper.
- `scripts/test-headroom-rate-limit-burst.mjs`: send a parallel invalid-key burst through the real Anthropic handler without model billing and fail on any local 429.
- `scripts/install-claude-rtk.ps1`: install pinned native Windows and WSL RTK, configure accuracy exclusions and `RTK.md`, and install the MSYS-safe global Claude Code Bash rewrite hook.
- `scripts/install-claude-rtk.sh`: idempotently install pinned RTK on a native Linux Claude Code host, preserve existing settings and non-RTK hooks, configure the same accuracy profile, and verify rewrite/exclusion behavior before returning success.
- `scripts/verify-claude-code-sub2api.ps1`: verify Headroom health/upstream, host bind state mounts, shared RTK history, the Git Bash -> WSL hook protocol, persistent memory/cache mounts, embedding-server logs/socket/factory/direct embed probe, sub2api health, Claude Code settings, and expected upstream model behavior.
- `scripts/ensure-sub2api-proxy-stack.ps1`: fast scheduled-task target that syncs the host Codex auth file, probes the Windows and Hyper-V routes, logs state transitions, and invokes full recovery only when required.
- `scripts/start-sub2api-proxy-stack.ps1`: idempotent recovery implementation that starts the WSL Docker compose stack, refreshes Claude Code `ANTHROPIC_BASE_URL` from WSL IP when needed, self-heals stale WSL VHDX attach locks, and optionally refreshes a Hyper-V VM bridge from `hyperv-bridge.env`.
- `scripts/test-autostart-self-heal-contract.ps1`: source contract for the repeating trigger, retry policy, health-first ordering, bridge verification, and nested recovery return behavior.
- `scripts/test-hyperv-headroom-bridge-contract.ps1`: fail-closed source contract for current switch/VM/WSL discovery, stale portproxy removal, VM-scoped firewall, atomic remote settings update, and VM-side health proof.
- `scripts/watch-claude-proxy-stack.ps1`: one-shot or bounded manual health diagnostic with optional `-RequireCuda`; never register it as a second autostart owner.
- `scripts/install-sub2api-autostart-task.ps1`: installs the single repeating `Sub2API Codex Proxy Stack Autostart` self-heal task with `RunLevel=Highest`, routes it through the zero-window `run-hidden.vbs` launcher, removes stale `headroom-proxy`, and disables Startup-folder proxy launchers.
- `scripts/install-claude-compact-recovery.ps1`: install Claude Code compact recovery hooks.
- `scripts/compact-recovery.mjs`: lightweight compact recovery hook implementation.

## Minimum Done Criteria

For install/config/debug tasks, do not call it done until these are true or explicitly blocked:

- `headroom-sub2api` and `sub2api-codex` containers are healthy.
- Headroom `/health` reports ready and upstream `http://sub2api:8080`.
- Headroom image has `/usr/local/bin/start-headroom-proxy` and `/opt/headroom-seed` so fresh persistent volumes are seeded without overwriting existing memory.
- When `.env` selects CUDA, Docker exposes a GPU to `headroom-sub2api`, Torch reports CUDA and the expected device, Kompress preloads as `pytorch`, and `benchmark-headroom-kompress --require-cuda` completes with full sentinel retention.
- Headroom embedding-server logs show `Embedding server: ready.`, no per-worker fallback or missing watchdog module, `/tmp/headroom-embed-8787.sock` exists, and the memory factory returns `headroom.memory.adapters.watchdog SocketEmbedderClient 384`.
- Headroom Claude Code streaming-overlap/watchdog patch is present and tested: `python deploy\claude-code-codex-headroom\test_headroom_claude_code_streaming_patch.py` passes, the installed handler has no `return JSONResponse(content=queued, status_code=202)`, an overlap probe logs `mid_turn_overlap_wait` / `mid_turn_overlap_wait_done` instead of returning private `headroom_queued` to Claude Code, and the handler watchdog live probe prints `WATCHDOG_RETRY_OK` proving a hung primary handler is retried once through Headroom bypass/passthrough before any 504 is returned.
- A forced `memory_search + Bash` Claude Code prompt from the real host completes without `No tool output found`, logs `Memory: Deferred 1 client-owned tool call(s) from continuation: ['Bash']`, and returns both results without leaking `memory_search` to Claude Code as an unavailable tool.
- Headroom, sub2api, Postgres, and Redis state mounts are Docker `bind` mounts to host paths, not Docker named volumes; if memory traffic has already happened, `/root/.headroom/ccr_store.db` exists and is non-empty.
- Every OS that directly runs Claude Code Bash has pinned RTK and exactly one Claude Code Bash hook. Windows-to-WSL hooks are MSYS-safe; native Linux hooks use the absolute host binary. A real fresh Claude Code Bash call creates a new RTK history row, accuracy-excluded commands remain raw, and Headroom `/root/.local/share/rtk` reports the same command/savings totals as host `rtk gain --format json` when the history is shared.
- On Windows/WSL installs, exactly one user autostart remains: `Sub2API Codex Proxy Stack Autostart`, `LastTaskResult=0`, `RunLevel=Highest`, action uses `wscript.exe` plus `run-hidden.vbs` and points at the canonical health-first ensure script, and any old `headroom-proxy` task or Startup-folder launcher is absent/disabled.
- `claude mcp list` shows `headroom` connected through Docker, not a missing host executable; stale host `tokensave` MCP is removed unless deliberately installed on host.
- `docker exec headroom-sub2api headroom tools doctor` shows RTK-related bundled tools available, and `headroom savings --json` / `headroom perf --format json` prove the optimization layer is recording traffic; `perf.cli_filtering.tool` is `rtk` with nonzero commands and savings after real Claude Bash traffic.
- A tiny `/v1/messages` request through `http://127.0.0.1:8787` succeeds for a GPT/Codex model such as `gpt-5.6-sol`; if testing compact/small-fast/subagents, Qwen succeeds with `reasoning_effort=high`.
- A tiny `/v1/messages` request through Headroom succeeds for an Alibaba Token Plan model such as `qwen3.8-max-preview`; `usage_logs` shows `account_id` for the Alibaba account, platform `anthropic`, and no `pricing not found` error. A tiny `/v1/messages/count_tokens` request for the same model returns `{"input_tokens":...}` locally and does not log `platform=openai` / `account_id=1`.
- A fresh `claude -p` request, including one with explicit `--model gpt-5.6-sol`, reaches `usage_logs` as `requested_model=qwen3.8-max-preview`, `reasoning_effort=high`, Alibaba account id/platform, and a `claude-cli/... (external, sdk-cli...)` user agent. Alibaba passthrough may leave `usage_logs.upstream_model` empty, so do not use that field alone as a failure signal. A control request with `(external, cli)` and `gpt-5.6-sol` must remain on GPT; otherwise the server rule is too broad.
- `/v1/models` through Headroom should publish GPT/Codex plus Alibaba Token Plan IDs only: no raw `opus`, `sonnet`, `haiku`, `fable`, or `claude-*` aliases. Claude Code Opus/Fable/Sonnet/Haiku picker slots are controlled by `ANTHROPIC_DEFAULT_*` env and should point to Qwen high.
- `~/.claude/settings.json` and User env agree on main/small models and Claude Code client context target.
- Every discovered Windows, native Linux, and WSL Claude user passes the portable subagent-profile check. For a Hyper-V Windows guest without remote execution, require both the host `hyperv-qwen-*.staged` marker and the guest `C:\ProgramData\sub2api\sync-claude-subagent-profile.log` after its next logon before calling guest application proven; then restart Claude and verify one real `Agent(...)` row on Qwen high.
- Any GitHub issue or fork change the user asked for is pushed and linked.
- The final report names the applicable `Fxx` incident IDs and includes evidence from the original client path. Source/config assertions without a rebuilt runtime and live request do not satisfy this criterion.
