{
  "skill_name": "sub2api-claude-code-codex",
  "evals": [
    {
      "id": 1,
      "prompt": "Поставь мне sub2api в Docker/WSL, чтобы Claude Code ходил через мою Codex подписку, модель gpt-5.6 full power, и проверь /context.",
      "expected_output": "Provides Docker/WSL install steps for the Headroom-first compose chain, builds/uses headroom-sub2api:0.31.0 with headroom-ai[proxy,code,relevance,html,spreadsheet,otel,reports,mcp] plus RTK, lean-ctx, TokenSave, ast-grep, difft, scc, and the downstream embedding-server patch in front of sub2api, configures Claude Code ANTHROPIC_BASE_URL to http://127.0.0.1:8787 while keeping sub2api :18081 for admin/diagnostics only, registers the headroom MCP through Docker and removes stale missing host headroom/tokensave MCP entries, persists all service state on host bind mounts under SUB2API_STATE_ROOT rather than Docker named volumes, persists Headroom memory/embeddings via /root/.headroom and warmed model caches via /root/.cache/headroom plus /root/.cache/huggingface, exposes only GPT/Codex plus Alibaba Token Plan names in /v1/models, routes GPT/Codex to OpenAI/Codex OAuth and qwen/glm/deepseek-v4-pro to the Alibaba Token Plan Anthropic-compatible account, uses gpt-5.6-sol for main work, uses qwen3.8-max-preview high for small-fast/compact/subagents, pins Claude Code Opus/Fable/Sonnet/Haiku picker aliases to Qwen high with ANTHROPIC_DEFAULT_OPUS_MODEL=qwen3.8-max-preview, ANTHROPIC_DEFAULT_FABLE_MODEL=qwen3.8-max-preview, ANTHROPIC_DEFAULT_SONNET_MODEL=qwen3.8-max-preview, ANTHROPIC_DEFAULT_HAIKU_MODEL=qwen3.8-max-preview, display name Qwen 3.8 Max, sets group messages_dispatch_model_config.compact_mapped_model=qwen3.8-max-preview so GPT/Codex compact requests cross-route before provider classification, disables CPU Kompress with HEADROOM_DISABLE_KOMPRESS=1 when Docker GPU proof fails, sets only a soft startup effortLevel=xhigh, clears persistent CLAUDE_CODE_EFFORT_LEVEL so /effort remains usable, verifies GPT-5.6 explicit --effort max requests reach usage_logs.reasoning_effort=max, sets CLAUDE_CODE_MAX_CONTEXT_TOKENS=370000 and CLAUDE_CODE_AUTO_COMPACT_WINDOW=340000 for the Claude Code safe compact profile, and verifies Headroom health/upstream, image bootstrap, Docker inspect Type=bind state mounts, ccr_store.db, embedding-server logs/socket/SocketEmbedderClient, headroom tools doctor/savings/perf, sub2api health, /context, /v1/models count 23 without Claude/Fable provider aliases, Qwen/GLM/DeepSeek usage, and usage logs.",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Claude Code показывает gpt-5.6-sol, но /context все равно 17k/200k. Почини и не путай client target с model window.",
      "expected_output": "Explains that Claude Code's displayed denominator is controlled by CLAUDE_CODE_MAX_CONTEXT_TOKENS and CLAUDE_CODE_AUTO_COMPACT_WINDOW, while GPT-5.6's configured model window is 1.05M. Sets CLAUDE_CODE_MAX_CONTEXT_TOKENS=370000 and CLAUDE_CODE_AUTO_COMPACT_WINDOW=340000 for the current safe compact profile, restarts Claude Code, verifies /context matches the configured client target instead of /200k, and checks logs for real context_length_exceeded before claiming an upstream ceiling.",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Настрой Claude Code picker так, чтобы остались GPT family и Qwen/Alibaba Token Plan, а Opus/Fable/Sonnet были Qwen high.",
      "expected_output": "Enables the explicit GPT/Codex plus Alibaba Token Plan models list, leaves GPT/Codex exact mappings on the OpenAI/Codex route, adds Alibaba Token Plan passthrough models such as qwen3.8-max-preview/glm-5.2/deepseek-v4-pro, removes raw opus/sonnet/haiku/fable/claude-* provider aliases from /v1/models, keeps only the narrow qwen3.8-max-preview to gpt-5.6-sol automatic-route availability candidate, sets require_oauth_only=false for the mixed-provider group, sets Claude Code ANTHROPIC_DEFAULT_OPUS_MODEL/ANTHROPIC_DEFAULT_FABLE_MODEL/ANTHROPIC_DEFAULT_SONNET_MODEL/ANTHROPIC_DEFAULT_HAIKU_MODEL to qwen3.8-max-preview with display name Qwen 3.8 Max, verifies /v1/models through Headroom returns 23 models with no Claude/Fable provider aliases, verifies a GPT request uses the OpenAI/Codex account, and verifies Qwen/GLM/DeepSeek requests use the Alibaba Token Plan Anthropic-compatible account with no pricing-not-found error.",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Claude Code через sub2api иногда дает пустые ответы и в usage_logs вижу 0/0 tokens. Разбери и почини full power профиль.",
      "expected_output": "Checks Claude JSONL plus sub2api usage_logs for stream=true 0/0 ghost streams, keeps MAX_THINKING_TOKENS=8000 as a Claude Code compatibility guard, explains that Codex reasoning is requested by effort=max and GPT-5.6 should reach upstream reasoning.effort=max while legacy models may downgrade to xhigh, and tells the user to restart/fork old Claude sessions.",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Claude Code пишет API Error: 503 Service temporarily unavailable, но sub2api /health живой. Отследи ошибку на прокси.",
      "expected_output": "Checks Headroom health/upstream first, then sub2api logs for openai_messages.account_select_failed/no available accounts before status_code=503 or 429, queries ops_error_logs and accounts for upstream status plus rate_limited_at/rate_limit_reset_at/temp_unschedulable_reason, distinguishes quota 429 from transient OAuth 403 and from context-window, ghost-stream, or Headroom routing failures, verifies adaptive OAuth 403 auto-heal when applicable, and recommends waiting for a real reset, reducing fan-out, or adding another valid OpenAI/Codex account instead of only raising concurrency.",
      "files": []
    },
    {
      "id": 6,
      "prompt": "Сделай компактизацию Claude Code через sub2api на qwen3.8-max-preview high: основная модель gpt-5.6-sol, а compact и subagents пусть уходят на Qwen high.",
      "expected_output": "Keeps ANTHROPIC_MODEL on gpt-5.6-sol, sets ANTHROPIC_SMALL_FAST_MODEL=qwen3.8-max-preview, sets CLAUDE_CODE_SUBAGENT_MODEL=qwen3.8-max-preview, patches user-level general-purpose/Explore/workflow-subagent/bench-reviewer/bench-triage frontmatter to model qwen3.8-max-preview with effort high, keeps Opus/Fable/Sonnet/Haiku picker aliases on qwen3.8-max-preview, configures only the qwen3.8-max-preview to gpt-5.6-sol availability fallback candidate for automatic routes, configures group messages_dispatch_model_config.compact_mapped_model=qwen3.8-max-preview for cross-provider compact reroute before route classification, restarts sub2api when code/config changed, and confirms healthy compact/subagent routing stays Qwen/high while terminal quota or its persisted quota circuit retries once on Sol/high without affecting direct interactive Qwen or generic scheduler misses.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "Почини Headroom/sub2api persistence так, чтобы я больше не пинал за Docker volumes и чтобы эмбеддинги не терялись.",
      "expected_output": "Proactively audits Docker inspect mounts before claiming done, refuses Docker named volumes for state, configures SUB2API_STATE_ROOT host bind mounts for Headroom /root/.headroom, /root/.cache/headroom, /root/.cache/huggingface, sub2api /app/data, Postgres data, and Redis /data, keeps the start-headroom-proxy bootstrap seed path, recreates only affected services, verifies Type=bind for every stateful container, verifies CCR_STORE True with nonzero size after memory traffic, verifies embedding-server SocketEmbedderClient and EMBED_OK, syncs the installed local skill, and commits/pushes repo skill/docs/script changes.",
      "files": []
    },
    {
      "id": 8,
      "prompt": "Мне нужен GPU для Headroom в Docker/WSL. Настрой, сравни с CPU и докажи, что Kompress реально работает на видеокарте.",
      "expected_output": "Keeps CPU as the portable default, selects the opt-in gpu image stage and docker-compose.gpu.yml on NVIDIA hosts, installs the pinned official CUDA PyTorch wheel, persists HEADROOM_ACCELERATOR=cuda plus HEADROOM_DOCKER_TARGET=gpu and HEADROOM_KOMPRESS_BACKEND=pytorch, proves non-empty Docker DeviceRequests, torch.cuda.is_available(), the expected device name, and Kompress preload backend pytorch, runs benchmark-headroom-kompress on identical CPU/GPU payloads, compares latency, throughput, compression ratio, and sentinel retention, verifies a real Headroom request and executor health, syncs repo/local skills, and does not claim that GPU alone solves partial-result compression, per-session singleflight, or unsafe oversized fail-open.",
      "files": []
    },
    {
      "id": 9,
      "prompt": "После перезагрузки Sub2API Codex Proxy Stack Autostart падает с Wsl/Service/0x8007274c, хотя контейнеры потом живые. Почини без второго автозапуска.",
      "expected_output": "Finds the exact scheduled-task log, normalizes UTF-16/NUL WSL output before classifying it, retries transient Wsl/Service/0x8007274c connection failures on every WSL command within a bounded deadline, preserves the single scheduled-task owner and GPU compose overlay, runs the real Scheduled Task, requires LastTaskResult=0, and rechecks Headroom health, Docker DeviceRequests, and CUDA after the task path.",
      "files": []
    },
    {
      "id": 10,
      "prompt": "RTK способен сильно резать вывод, но Headroom показывает RTK 0 commands / 0 tokens saved. Настрой автоматическое участие в Claude Code и закрепи в skill.",
      "expected_output": "Explains that container-only RTK cannot rewrite Claude Code Bash commands executed on Windows, installs pinned RTK on Windows plus WSL, keeps exactly one global PreToolUse(Bash) bridge with MSYS2_ARG_CONV_EXCL so Git Bash cannot corrupt Linux paths, preserves cat/git diff/git show/curl accuracy exclusions and RTK_DISABLED bypass, mounts the host RTK history into Headroom /root/.local/share/rtk, verifies the bridge through Git Bash rather than direct PowerShell-to-WSL, proves a real fresh Claude Code Bash tool call creates a new history.db row and matching host/container gain totals, verifies headroom perf cli_filtering is nonzero, and does not present a manual container benchmark as automatic live savings.",
      "files": []
    },
    {
      "id": 11,
      "prompt": "Claude Code стоит на хостовой Ubuntu в Hyper-V VM, а не в devcontainer. Поставь и проверь RTK именно там.",
      "expected_output": "Uses scripts/install-claude-rtk.sh on the native Linux host as the Claude user, pins RTK 0.42.4 under ~/.local/bin, preserves existing settings/env and non-RTK hooks, leaves exactly one absolute PreToolUse(Bash) RTK hook, configures cat/git diff/git show/curl exclusions, distinguishes the host filesystem from devcontainers, restarts or launches a fresh Claude process, and proves automatic participation with Bash stream-json, hook debug modified-input evidence, and an increment in rtk gain rather than only a manual RTK command.",
      "files": []
    },
    {
      "id": 12,
      "prompt": "Один OpenAI OAuth 403 HTML отрубил все окна Claude Code на десять минут: health живой, но везде no available accounts и 503. Сделай автохил без ручного SQL reset.",
      "expected_output": "Proves the upstream 403 and account temp_unschedulable state from logs/Postgres, verifies the running fork uses 2s/15s/1m/5m consecutive OAuth cooldowns rather than a fixed ten-minute first-hit block, retries the first /v1/messages OAuth 403 once after 2.25s inside the original request, resets the consecutive counter and clears only the OAuth 403 temp state after successful OpenAI usage, preserves unrelated model/quota limits, runs focused service/handler regression tests, rebuilds and recreates only sub2api, and verifies a live request through Headroom plus the openai_403_autoheal log markers.",
      "files": []
    },
    {
      "id": 13,
      "prompt": "Я сбросил недельный Codex лимит на сайте OpenAI, но Claude Code продолжает получать старый 429 rate-limited until через несколько дней. Почини сам stale lock, без fallback на другую модель.",
      "expected_output": "Proves that accounts.extra.model_rate_limits still contains the old quota-origin reset while the user performed an external reset, does not add or change model fallbacks, makes a successful proxy reset-credit clear persisted and scheduler-cached model cooldown state immediately, allows a bounded same-model reprobe one minute after rate_limited_at for external site resets, re-closes the probe window on a fresh real upstream 429, clears the stale entry after a successful fresh Codex snapshot, adds boundary and mutation-killing tests, rebuilds only sub2api, and verifies Claude Code gpt-5.6-sol success with usage_logs.model_mapping_chain gpt-5.6-sol->gpt-5.6-sol plus an empty model_rate_limits object.",
      "files": []
    },
    {
      "id": 14,
      "prompt": "Claude Code в Hyper-V VM 25 минут ретраил Unable to connect to API (ConnectionRefused): WSL получил poweroff уже после успешного logon-autostart. Сделай умный автохил, чтобы это больше не требовало ручного запуска WSL.",
      "expected_output": "Proves the outage timeline from the Claude session, Linux journal, Docker start times, Windows portproxy, and Scheduled Task state; distinguishes pre-HTTP ConnectionRefused from Headroom/sub2api/provider errors; keeps one elevated Scheduled Task owner; replaces the logon-only action with a health-first ensure script triggered at logon and every minute; configures at least three one-minute task retries with IgnoreNew; performs no container restart on a healthy check; on failure wakes WSL, restores compose and the dynamic Hyper-V bridge through the canonical start script, verifies same-host and VM health, records recovery_started and recovered events, adds a fail-closed contract test, and proves a controlled WSL/bridge outage recovers without manual intervention.",
      "files": []
    },
    {
      "id": 15,
      "prompt": "Claude Code получил 503, а в sub2api OpenAI account status=error: OPENAI_OAUTH_TOKEN_REFRESH_FAILED refresh_token_reused. Сделай автоматический хил, чтобы без ручного SQL после codex login всё поднималось само.",
      "expected_output": "Proves the root cause from Claude JSONL, ops_error_logs, and accounts status/schedulable fields; distinguishes this from Headroom local 429, quota cooldown, context overflow, and upstream 403; configures SUB2API_OPENAI_CODEX_AUTH_FILE=/app/data/codex-auth.json and the self-heal task to copy a validated host %USERPROFILE%\\.codex\\auth.json into the sub2api host bind mount without printing secrets; implements backend recovery that imports a fresh non-expired auth file on refresh_token_reused/invalid_refresh_token-style failures, clears status=error, clears temp-unschedulable, sets schedulable=true, invalidates stale token cache, retries OpenAI account selection once in the original request, and returns 401 authentication_error instructing codex login when the file is missing/unchanged/expired instead of masking it as 503; covers token refresh, selection retry, no-account classification, compose env, setup, and autostart scripts with focused tests; rebuilds/recreates sub2api and verifies health plus a live Headroom request or truthful 401 if the host auth file is stale.",
      "files": []
    },
    {
      "id": 16,
      "prompt": "Я вызвал /compact, но он шёл несколько минут и в логах вижу gpt-5.6-sol. Проверь, какая модель реально делала compact, и почини маршрут.",
      "expected_output": "Classifies the incident as F05, locates the exact Claude compact request and timestamps, proves whether Headroom preserved the final compact prompt and emitted x-sub2api-claude-compact, correlates the request to sub2api usage_logs instead of trusting the progress UI, fixes marker preservation and pre-provider compact remapping when stale, rebuilds/recreates the affected runtime, and verifies a real /compact reaches qwen3.8-max-preview with reasoning_effort=high on the Alibaba account while the main model remains unchanged.",
      "files": []
    },
    {
      "id": 17,
      "prompt": "В чистой Claude Code сессии первый запрос падает: 400 No tool output found for function call call_*. Найди точный запрос и исправь, без советов просто повторить.",
      "expected_output": "Classifies the incident as F16, finds the exact Claude JSONL call ID and corresponding Headroom continuation, checks for a mixed memory_search plus client Bash/Read turn, fixes Headroom to defer client-owned calls and replay only server calls with matching results while removing private memory definitions, runs the focused mixed-memory regression, rebuilds/recreates Headroom, and proves the real fresh prompt completes with the deferred-client-call log marker and a sub2api 200 continuation.",
      "files": []
    },
    {
      "id": 18,
      "prompt": "Claude пишет 429 Retry after 0.3s при параллельных агентах. Не сваливай это на лимит подписки: выясни слой и почини так, чтобы потерянный tool call не ронял агента.",
      "expected_output": "Classifies the incident as F12 until evidence shows an upstream 429, correlates whether the request stopped at Headroom's local rate limiter or reached sub2api/provider, checks effective /stats RPM and TPM, keeps provider cooldown logic separate, configures the loopback profile to 6000 RPM and 100000000 TPM, recreates Headroom, runs the parallel burst probe, and verifies Claude Code no longer receives a local tool-result 429 under the tested fan-out.",
      "files": []
    },
    {
      "id": 19,
      "prompt": "Сабагент остановился с Stream ended without receiving any events / incomplete chunked read. Сделай так, чтобы автономность не ломалась, но не маскируй невалидный SSE слепым retry.",
      "expected_output": "Classifies the incident as F13, captures the raw Anthropic SSE and correlates Claude, Headroom, sub2api, and provider request IDs, distinguishes a pre-first-event disconnect from a mid-stream partial response, validates required event ordering and serialization, repairs the invalid stream at its producing layer, permits one internal retry only for replay-safe pre-event requests, adds malformed/empty/partial stream regressions, rebuilds the runtime, and proves one complete Claude Code continuation without duplicate tool execution.",
      "files": []
    },
    {
      "id": 20,
      "prompt": "Claude завис на plugin:context-mode:ctx_batch_execute на несколько часов. Проверь, это Headroom/sub2api или сам MCP, и не трогай прокси без доказательств.",
      "expected_output": "Classifies the incident as F15, compares MCP call timestamps with Headroom/sub2api traffic and treats absence of a proxy request as evidence against the proxy, inspects command versus indexing/search/formatting phases, verifies the whole-handler watchdog and bounded markdown chunking in the installed context-mode runtime, reproduces a forced hang with a short watchdog, and reports the plugin fix separately from a control request proving Headroom remains healthy.",
      "files": []
    },
    {
      "id": 21,
      "prompt": "Один general-purpose агент породил сотни сабагентов. У меня workflowSizeGuideline=small, почему лимит не сработал и какие модели/effort реально использовались? Только анализ и отчёт.",
      "expected_output": "Classifies the incident as F17, does not claim workflowSizeGuideline is a hard limit, does not install a blocking Agent hook or change runtime during an analysis-only request, reconstructs the parent-child agent graph from Claude JSONL, reports launch count, peak concurrency, nesting, model, effort, duration, token use, terminal state, and 429/502/503 distribution, and separates configured Qwen-high overrides from actual usage_logs evidence.",
      "files": []
    },
    {
      "id": 22,
      "prompt": "/effort xhigh говорит, что CLAUDE_CODE_EFFORT_LEVEL=max overrides this session, а я хочу переключать effort интерактивно. Найди весь хардкод и докажи upstream effort.",
      "expected_output": "Classifies the incident as F07, searches Windows User/System env, Claude settings, launchers, scheduled tasks, VM shell profiles, agent frontmatter, and Headroom routing, removes persistent CLAUDE_CODE_EFFORT_LEVEL without removing a user-selected soft effortLevel, keeps HEADROOM_EFFORT_ROUTER=0, restarts a fresh Claude process, and proves two selected efforts from a pre-Headroom body capture plus matching sub2api usage_logs rather than UI labels.",
      "files": []
    },
    {
      "id": 23,
      "prompt": "После перезагрузки в отчёте снова написано Kompress disabled/CPU, хотя GPU уже настраивали. Найди регрессию, не верь nvidia-smi и проверь реальный автозапуск.",
      "expected_output": "Classifies the incident as F20 and F21, inspects the canonical Scheduled Task action and effective compose file list, refuses to classify a failed WSL docker inspect as CPU, preserves sticky cuda state, removes stale bare-compose launch paths, requires GPU DeviceRequests, Torch CUDA/device, Kompress preload=pytorch, benchmark sentinel retention, live compression metrics, and LastTaskResult=0 after invoking the real autostart path, then syncs and pushes both repo skills when contracts changed.",
      "files": []
    },
    {
      "id": 24,
      "prompt": "Изучи архитектуру и функции прокси-конкурентов, анализ и отчёт only. Не запускай их тесты и ничего не устанавливай.",
      "expected_output": "Classifies the operation mode under F24, performs read-only architecture comparison by protocol translation, authentication/subscription support, streaming and tool-call fidelity, model mapping, compact handling, error propagation, maintenance recency, and existing test surface; it does not execute broad suites, install software, mutate runtime, or rank by stars alone, and clearly labels which claims come from source inspection versus live proof not performed.",
      "files": []
    },
    {
      "id": 25,
      "prompt": "Ты уже поправил исходники и тесты зелёные, но Claude Code всё ещё выдаёт старую ошибку. Проверь, почему фикс не работает вживую.",
      "expected_output": "Classifies the incident as F02 and F24, compares the changed commit with the running image labels/revision, container creation time, installed skill/cache copies, effective env and compose files, rebuilds and recreates only the affected services when requested, restarts a fresh Claude process when client state is sticky, and reproduces the original request end-to-end with a negative control instead of presenting source tests or health endpoints as runtime proof.",
      "files": []
    },
    {
      "id": 26,
      "prompt": "Claude написал Compacted, но через один запрос снова 0% until auto-compact и context limit. Докажи, compact не сработал или окно заново залили.",
      "expected_output": "Classifies the incident as F06, captures pre-compact context, compact response/summary size, post-compact context, and the first follow-up request; attributes rehydrated rules, skills, hooks, MCP schemas, task state, and tool outputs separately; checks whether compact actually routed via F05; avoids guessing from the terminal banner; and proposes a fix at the component that refilled the request, with a repeat compact plus one controlled follow-up as proof.",
      "files": []
    },
    {
      "id": 27,
      "prompt": "После рестарта Headroom /stats показывает только несколько сотен запросов текущего uptime, хотя proxy-requests.jsonl сохранён. Сделай lifetime статистику из логов и докажи, что она не обнуляется.",
      "expected_output": "Classifies the incident as F25, proves the mismatch between runtime /stats counters, persistent_savings, and the bind-mounted request JSONL; implements one-time startup hydration that streams the JSONL, skips malformed lines, deduplicates request IDs, restores the recent feed, and updates lifetime aggregates on append; exposes /stats.request_history plus nested requests/tokens/latency lifetime blocks while preserving runtime fields; keeps /stats/reset runtime-only; covers malformed, duplicate, truncated-tail, append, and cached-stats behavior; rebuilds/recreates Headroom; verifies identical totals and range across restart, exactly one new unique request after a live probe, and the same increment after a second restart.",
      "files": []
    },
    {
      "id": 28,
      "prompt": "Сделай так, чтобы не только Agent сабагенты, но и любой claude -p во всех хостах и VM шёл на qwen3.8-max-preview high, а обычный интерактивный Claude оставался переключаемым.",
      "expected_output": "Keeps user-level Agent overrides on qwen3.8-max-preview high, adds opt-in group messages_dispatch_model_config.sdk_cli_mapped_model=qwen3.8-max-preview and sdk_cli_reasoning_effort=high, identifies only claude-cli external sdk-cli traffic so Agent SDK and standalone claude -p/--print are rewritten before provider classification, applies the same model rule to count_tokens, preserves compact model precedence, does not rewrite external cli interactive requests, preserves the new fields through admin config round trips, rebuilds/recreates sub2api, and proves an explicit claude -p --model gpt-5.6-sol --effort max request logs requested_model Qwen, Alibaba account, and high effort while an interactive-user-agent control remains GPT. It does not require upstream_model for Alibaba passthrough because that field may be empty.",
      "files": []
    },
    {
      "id": 29,
      "prompt": "Qwen Token Plan выбил недельный лимит и /compact с сабагентами падают на 429. Сделай умный fallback на gpt-5.6-sol, но если Qwen работает, на GPT не переключай.",
      "expected_output": "Classifies the incident as F30, matches only status 429 with Throttling.AllocationQuota plus explicit token-plan quota exhaustion, parses and persists the provider UTC reset as an Alibaba account circuit, configures qwen3.8-max-preview to gpt-5.6-sol as a candidate, permits one pre-stream cross-provider retry at high effort only for compact and external sdk-cli automatic routes after terminal quota or while every matching Qwen account has that persisted circuit open, leaves a selected healthy Qwen and generic scheduler misses on Alibaba, leaves direct interactive Qwen and transient/context/auth/model/mid-stream failures unfallbacked, proves the first terminal request completes on Sol and the second causes no Qwen probe, covers malformed reset and negative classes, rebuilds/recreates the stack, syncs the installed skill, commits and pushes main.",
      "files": []
    },
    {
      "id": 30,
      "prompt": "Переключи вообще весь Claude Code на мою Anthropic подписку: main, compact, claude -p и всех сабагентов, без fallback в GPT/Qwen.",
      "expected_output": "Uses anthropic-only profile version 4, validates and imports the existing Claude Code OAuth into sub2api without exposing credentials, atomically rebinds the stable key to an Anthropic-only group, maps main/Opus and legacy Opus 4.8 to claude-opus-5, force-maps compact/small-fast/Haiku and stale Spark/Luna/mini/Qwen IDs to Sonnet 5, sets MAX_CONTEXT and AUTO_COMPACT to 1000000 on every reachable Claude host, keeps OpenAI and Alibaba out of the group, reconciles every reachable host/VM, marks offline nodes pending-reconcile, and proves main/compact/print/nested-agent traffic uses only the Anthropic account after the switch boundary. The compact proof deliberately submits adaptive thinking plus effort=max and must log claude-sonnet-5 with reasoning_effort=low.",
      "files": []
    },
    {
      "id": 31,
      "prompt": "Верни весь парк на прошлую схему GPT main плюс Qwen high для compact и сабагентов.",
      "expected_output": "Restores the versioned hybrid-current group snapshot rather than guessing settings, atomically switches the stable key first, reconciles host and VMs, preserves unrelated Claude configuration, and proves OpenAI main plus healthy Alibaba compact/SDK/subagent routing with only the profile's explicit terminal-quota fallback.",
      "files": []
    },
    {
      "id": 32,
      "prompt": "claude-route status: скажи, реально ли все сейчас на Anthropic, одна VM выключена.",
      "expected_output": "Reads the stable-key group binding and persisted generation, checks usage rows after the switch boundary, reports reachable nodes as synced or drifted and the offline VM as pending-reconcile, and does not infer provider state from Claude UI labels or settings files alone.",
      "files": []
    },
    {
      "id": 33,
      "prompt": "После переключения старая сессия явно просит qwen3.8-max-preview. Она не должна уйти в Alibaba.",
      "expected_output": "Verifies anthropic-only pre-provider force mapping for explicit stale Qwen/GPT IDs, runs a resumed-session probe through Headroom, and proves the request uses the Anthropic account with zero forbidden-provider usage rather than relying on client config synchronization.",
      "files": []
    },
    {
      "id": 34,
      "prompt": "Anthropic профиль не проходит первый live probe. Не оставляй мне полусломанное состояние.",
      "expected_output": "Restores the previous stable-key group binding, invalidates the routing cache, proves one request on the previous provider, leaves node configuration unchanged because the commit point was not reached, and reports the exact failed layer without leaking credentials.",
      "files": []
    },
    {
      "id": 35,
      "prompt": "/compact в anthropic-only падает с 400 This model does not support the effort parameter. GPT, Qwen и Spark сейчас в лимитах; сделай безопасный fallback на Sonnet 5.",
      "expected_output": "Correlates the failing compact row and proves it reached claude-haiku-4-5-20251001 on the native Claude OAuth account with inherited output_config.effort/adaptive thinking. Applies anthropic-only version 4 so compact, small-fast, Haiku compatibility, and stale Spark/Luna/mini IDs resolve directly to claude-sonnet-5 without involving GPT/Qwen accounts, upgrades main/Opus to claude-opus-5, owns the 1M client context fields, reapplies the profile generation, and verifies an effort=max adaptive compact-marker probe returns 200 with usage_logs model claude-sonnet-5 and reasoning_effort=low with no new Haiku/GPT/Qwen rows.",
      "files": []
    },
    {
      "id": 36,
      "prompt": "Вышел Opus 5: замени Opus 4.8 во всем anthropic-only профиле, а compact сделай на Sonnet 5 low, потому что он слишком долгий.",
      "expected_output": "Verifies the current official API IDs and the live native subscription, upgrades anthropic-only to version 4 with main/Opus/legacy Opus 4.8 aliases mapped to claude-opus-5, adds model metadata for its 1M input and 128k output limits, owns both 1M client context fields, adds compact_reasoning_effort=low beside compact_mapped_model=claude-sonnet-5, gives compact effort precedence over SDK CLI and inherited session effort, covers normalization plus an incoming max-to-low rewrite mutation, rebuilds/recreates sub2api, reapplies the live profile, and proves tagged usage rows show main claude-opus-5, compact claude-sonnet-5/low, SDK CLI claude-sonnet-5/high, and only the Anthropic OAuth account before syncing the installed skill and pushing main.",
      "files": []
    },
    {
      "id": 37,
      "prompt": "На Opus 5 Claude Code всё ещё показывает маленький контекст 340k/370k. У модели 1M, исправь на хосте и виртуалках.",
      "expected_output": "Classifies the stale client window as F34, proves settings.json, User/process env, and the wrapper retained hybrid 370000/340000 values, upgrades Claude Code to 2.1.219 or later for Opus 5, versions anthropic-only to v4 with 1000000 context values and gateway-facing claude-opus-5[1m]/claude-sonnet-5[1m] IDs while keeping server IDs suffix-free, preserves Opus 5 main plus Sonnet 5/low compact routing, migrates installed v3 artifacts, reconciles every reachable Windows/Linux/Hyper-V Claude host, marks unreachable nodes pending instead of falsely claiming sync, proves raw ID returns a 200000 client budget while [1m] returns 1000000, verifies sub2api received the stripped raw Opus 5 model, reruns provider route proof, and commits/pushes the unified skill profile.",
      "files": []
    },
    {
      "id": 38,
      "prompt": "API Error: Request rejected (429) · No available accounts: no available accounts. Разберись и пофикси, но anthropic-only не должен тихо уйти в GPT/Qwen.",
      "expected_output": "Classifies the incident as F37, correlates the generic no-account burst with the immediately preceding real Anthropic subscription-window 429 and the only allowed OAuth account's reset timestamp, proves the account later auto-restored without SQL clearing, preserves the diagnosed model/reset response instead of overwriting it, emits reset-derived Retry-After, keeps cross-provider fallback forbidden, and configures the paired Headroom held-SSE path so a real reset window is waited inside the original Claude turn with ping keepalives and one serialized probe. It covers message/header, long-delay, concurrency, deadline, cancellation, and compose contracts; rebuilds/recreates the live stack; and finishes with a controlled 429-to-200 recovery plus claude-route verify PASS.",
      "files": []
    },
    {
      "id": 39,
      "prompt": "Ночью Anthropic subscription ушла в 429 на 45 минут, Headroom сделал десять коротких ретраев, и все Claude Code агенты умерли. Сделай так, чтобы исходные turns сами дождались reset, без GPT/Qwen fallback и без retry storm.",
      "expected_output": "Traces the failure to F37 and distinguishes ordinary bounded retries from a real provider cooldown. Proves that sub2api already admits the account after the reset timestamp and clears persisted cooldown on the first successful Anthropic response, then implements the missing client-continuity layer: HEADROOM_UPSTREAM_429_HOLD_ENABLED returns HTTP 200 text/event-stream, emits valid Anthropic ping events before message_start, honors full Retry-After, and replays the exact request. A route-scoped circuit clock and one probe lock prevent a retry storm. After short retries, configured 429/502/503/504/529 and transport disconnects enter the same hold; 400/401 remain visible and no provider fallback is enabled. It proves 429-to-200, 503-to-200, transport-to-200, ten-client single-probe, deadline, cancellation, live health counters, and Claude ping-before-message-start, then rebuilds the pinned fork and syncs compose, skills, evals, runtime, commits, and pushes.",
      "files": []
    },
    {
      "id": 40,
      "prompt": "Ретраи не единственный self-heal: временный 503 или рестарт sub2api тоже не должен ронять автономного Claude Code агента. Доведи защиту до конца и дай живые метрики.",
      "expected_output": "Keeps ordinary short retries as the first layer, then converts exhausted 502/503/504/529 and transport errors into the same held Anthropic SSE stream used for subscription 429. It sends pings, retries safely only before upstream response bytes, shares a route/credential circuit and probe lock, preserves 400/401, cancellation, deadline, and anthropic-only isolation, and exposes active holds, cooling routes, next probe, recoveries, timeouts, cancellations, and transport failures in /health.runtime.upstream_recovery. It covers deterministic HTTP and transport recovery tests, compose/setup/verifier contracts, rebuilds the live WSL Docker image, demonstrates a held live 429 and controlled 503/transport recovery, syncs installed skills, and commits/pushes both forks.",
      "files": []
    },
    {
      "id": 41,
      "prompt": "После фикса held SSE Claude Code снова получил 429 rate-limited until на Opus 5 и остановил агента. Докажи точный request path и закрой пробой, а не просто увеличь retry.",
      "expected_output": "Correlates the exact claude-cli row through Headroom and sub2api, checks the inbound stream flag, and identifies the uncovered buffered stream:false compact/fallback/background path instead of claiming bypass or stale account state. It extends the shared route cooldown/singleflight recovery to HTTP 200 chunked application/json, emits only legal leading JSON whitespace while waiting, appends the recovered Anthropic JSON on the same connection, and covers configured 429/502/503/504/529 plus exhausted transport failures without cross-provider fallback. It preserves nonretryable 400/401, cancellation, and deadlines; adds JSON-framing and buffered-handler regression tests; runs a controlled live 429-to-200 buffered probe proving JSON parse plus holds_total=1/recoveries_total=1; rebuilds the pinned Headroom revision; verifies a real Claude CLI request; updates skills/evals/runtime; and commits/pushes both forks.",
      "files": []
    },
    {
      "id": 42,
      "prompt": "Переключи весь Claude Code fleet с native Anthropic на full ChatGPT/Codex. Сабагенты должны быть на gpt-5.6-luna xhigh, compact на Luna high, никаких Qwen/Claude fallback, и проверь это по живым логам.",
      "expected_output": "Uses chatgpt-only profile v3 and a dedicated chatgpt-subscription-only group containing only codex-chatgpt-subscription. Before switching, validates host ~/.codex/auth.json, recognizes a Linux absolute SUB2API_STATE_ROOT on Windows, translates the source with wslpath, atomically publishes mode-0600 codex-auth.json inside WSL, and proves host/WSL SHA-256 equality without exposing credentials. It binds the stable key transactionally, rolls back on a failed Headroom Sol probe, configures Sol for main/Opus, Terra for Fable/Sonnet, Luna for Haiku/small-fast, Luna/high for compact, Luna/xhigh for generic Agent SDK routes, and Sol/high only for the built-in Plan agent recognized by a fail-closed system-plus-UA-plus-tools classifier. It keeps generic model and group fallbacks empty, exposes GPT/Codex models only, maps stale Claude/Qwen/GLM/DeepSeek IDs to GPT roles, and preserves the v2 same-provider automatic Luna-to-Sol recovery for terminal empty-output compact/SDK/Haiku turns; healthy and direct interactive Luna remain Luna. It removes persistent CLAUDE_CODE_EFFORT_LEVEL while preserving interactive effortLevel, reconciles reachable Windows and Hyper-V nodes, and records offline nodes as pending. Verification requires five correlated 200 usage rows on the OpenAI OAuth account: Sol main, Luna stale-model, Luna/high compact, Luna/xhigh generic SDK, and Sol/high Plan; it also documents that Claude Code 2.1.219 labels standalone claude -p as external cli and Agent SDK as sdk-cli, so the proxy must not hijack every interactive CLI request.",
      "files": []
    },
    {
      "id": 43,
      "prompt": "Один Sonnet 429 был шесть часов, после переключения на ChatGPT мои Sol-запросы каждые пять минут зависали, хотя повтор через buffered CCR отвечал за секунды. Исправь без отключения recovery hold.",
      "expected_output": "Classifies the incident as F38, correlates the original Sonnet 429 route hash with inherited Sol streaming holds and fast buffered Sol controls, and identifies URL-plus-credential cooldown scope plus missing cross-path success cleanup. It keys Headroom cooldown and probe state by upstream URL, credential, and normalized requested model; passes the model through streaming, buffered, 429, 5xx, and transport hold constructors; clears only the matching model circuit after successful streaming or buffered responses; preserves same-model singleflight and long Retry-After continuity; proves Sonnet cooldown leaves Sol at zero, proves buffered success clears stale Sol state, keeps the existing recovery/cancellation/deadline suites green, pins the rebuilt Headroom fork SHA in sub2api, recreates the live GPU stack, and verifies a real multi-turn Claude Code request has no inherited five-minute hold.",
      "files": []
    },
    {
      "id": 44,
      "prompt": "В chatgpt-only старый Haiku запрос мапится в Luna, три раза получает пустой 502, потом no available accounts, а Headroom держит turn бесконечно. Исправь без скрытого fallback для обычной Luna.",
      "expected_output": "Classifies the incident as F39 and correlates the active Headroom buffered hold with sub2api empty-output 502, three same-account retries, account failover, and no second account. Upgrades chatgpt-only to profile v2 while keeping generic model_fallbacks empty, adds automatic_model_fallbacks gpt-5.6-luna to gpt-5.6-sol, preserves the original pre-alias model plus automatic compact/SDK marker in Gin context, and switches a buffered empty-output automatic or Haiku small-fast turn to Sol before same-account retries. It proves direct interactive and healthy Luna stay Luna, Anthropic/Alibaba remain excluded, adds handler/service/profile mutation tests, rebuilds only sub2api, reapplies the profile, observes the held request recover, reruns provider and full-stack verification, syncs installed skills, and commits/pushes main.",
      "files": []
    },
    {
      "id": 45,
      "prompt": "Сделай так, чтобы только встроенный Plan агент Claude Code работал на gpt-5.6-sol high, а обычные сабагенты оставались на Luna xhigh. Не трогай Claude Code конфиги и не верь одному User-Agent.",
      "expected_output": "Implements the policy entirely in sub2api group configuration with plan_mapped_model=gpt-5.6-sol and plan_reasoning_effort=high while preserving sdk_cli_mapped_model=gpt-5.6-luna and sdk_cli_reasoning_effort=xhigh plus compact Luna/high precedence. It first captures real Plan and control requests, classifies only from the structured system field plus claude-cli external sdk-cli User-Agent, non-empty metadata.user_id, required read-only tool names, and absence of Edit/Write, never from user messages or whole-body substrings. It keeps a bounded TTL positive-only session cache keyed by a hash of metadata.user_id for shortened continuation prompts, fails closed to generic SDK after restart or signature drift, covers anchor/tool/UA/session mutations and prompt injection, preserves all new fields through admin and profile round trips, rebuilds sub2api, and proves live Plan Sol/high, generic SDK Luna/xhigh, and compact Luna/high from usage_logs plus claude_code.agent_role_route logs.",
      "files": []
    }
  ]
}
